#!/usr/bin/env python

import sys,string
import argparse
import toolbox

parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter)
parser.add_argument('chars', default = 'astro-toolbox works')
args        = parser.parse_args()

toolbox.hello(args.chars)

